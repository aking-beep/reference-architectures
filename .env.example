# Reference Architectures is a free, open catalog — no access gate, no account required.

# Optional: forward update-list signups to a webhook (best-effort, never blocks browsing).
# EMAIL_CAPTURE_WEBHOOK_URL=
# LEAD_WEBHOOK_URL=

# Optional durable rate-limit store (falls back to in-memory).
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# NEXT_PUBLIC_BASE_URL=https://referencearchitectures.arctransformationgrouplab.dev
